# Закладка в алгоритме шифрования


## Принцип работы

По умолчанию шифрует с помощью шифра сдвига<br/>
При передаче флага шифрует с помощью aes-192-cbc<br/>


## Запуск
```
node main.js, при стандартном шифровании
node main.js -m, при альтернативном шифровании
```